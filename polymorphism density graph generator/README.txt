python program will take the name of the VCF file, the window size, and the increment value as command
line arguments.
eg. generate_density_data.py <vcf file> <window size> <increment value>
The program will output to a file the data used to generate the polymorphism density plot